/**
 * @deprecated - Formation preferences are now handled conversationally by Gemini.
 * This component is kept to prevent import errors but should be removed in future cleanup.
 */
export function FormationSettings() {
    return null;
}
